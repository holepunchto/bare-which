# bare-which

Find a program in `$PATH`. Based on [node-which](https://github.com/npm/node-which).

```
npm i bare-which
```

## Usage

```js
const which = require('bare-which')

await which('ping')
// => /bin/ping
```

## License

Apache-2.0
